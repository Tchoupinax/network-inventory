name: Build

on:
  push:
    branches:
      - feature/**

jobs:
  docker:
    name: "👷 Build"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Use Node.js 25
        uses: actions/setup-node@v6
        with:
          node-version: 25

      - run: yarn && yarn build
